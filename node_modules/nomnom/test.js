var options = require("./nomnom").opts({
      apple: {
         string: "-a, --apples",
      },
      pear: {
         string: "--pear",
         help: "pears to add"
      },
      ip : {
         string: "-h ADDRESS, --host=ADDRESS",
         default: "127.0.0.1"
      }
   }).parseArgs();
   
console.log(options);